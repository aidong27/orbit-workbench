# 可选：覆盖自动发现的 Grok Build 二进制绝对路径。
# 此示例文件不会被应用自动加载；请在启动星轨工作台前设置运行时环境变量。
# macOS / zsh: export GROK_BINARY="/Users/your-name/.grok/bin/grok"
# Windows / PowerShell: $env:GROK_BINARY = 'C:\Users\your-name\.grok\bin\grok.exe'
