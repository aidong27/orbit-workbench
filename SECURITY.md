# 安全说明

当前仓库处于私有预览阶段。请不要在公开 Issue、截图或日志中提交 API 密钥、OAuth 令牌、私有仓库内容或本地绝对路径。

## 设计原则

- Grok Build 认证由官方 CLI 处理。
- 渲染进程不具备 Node.js 或任意 shell 权限。
- 文件夹选择、Git 检查和 ACP 子进程都在 Electron 主进程中完成。
- IPC 参数在主进程重新验证；Git 使用固定程序与参数数组。
- 权限请求由 ACP 传入并由用户明确选择，不在界面层伪造“默认允许”。

## 开源前必须完成

- 运行依赖漏洞与许可证检查。
- 对完整 Git 历史执行秘密扫描。
- 复核所有截图和示例路径。
- 明确许可证、商标说明与安全报告渠道。
- 完成 macOS 签名、公证和自动更新威胁建模后，才能提供正式安装包。
